<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GOALSPIN">
        <!-- 1. หน้า Splash (ใส่ intent-filter ไว้ที่นี่แทน) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. หน้า Main (เอา intent-filter ออก เหลือแค่นี้) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity android:name=".DailyQuestActivity" />
        <activity android:name=".InventoryActivity" />
        <activity android:name=".CollectionDefaultActivity" />
        <activity android:name=".CollectionForestActivity" />
        <activity android:name=".CollectionPortActivity" />
        <activity android:name=".CollectionCastleActivity" />
        <activity android:name=".CollectionMountainActivity" />

    </application>

</manifest>